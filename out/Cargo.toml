[workspace]
members = [  # TODO: figure out how to fix this so i can cargo clean && make test
    "gen/linrays",
    "gen/tres",
    "gen/sanity",
    "gen/mandelbrot",
    "gen/stamp_pos",
    "gen/async",
]
resolver = "2"

[profile.release]
panic = "abort"
strip = "debuginfo" # true
# `lto = true
